# discord-birthday-clown
A silly Discord webhook to celebrate my friend's birthday on a server

* `clown.py` is the main code
* `fetcher.py` is a util code to fetch birthday from an iCal URL
* `wishes.py` is another util for wish variations

